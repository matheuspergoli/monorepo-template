{
	"$schema": "https://schema.railpack.com",
	"provider": "bun",
	"packages": {
		"bun": "latest"
	},
	"steps": {
		"install": {
			"inputs": [
				{
					"local": true,
					"include": [".", "pnpm-workspace.yaml", "pnpm-lock.yaml"]
				}
			],
			"caches": ["pnpm-store"],
			"commands": ["corepack enable && pnpm install --frozen-lockfile"]
		},
		"build": {
			"inputs": [{ "step": "install", "include": ["."] }],
			"caches": ["pnpm-store"],
			"commands": ["corepack enable && pnpm --filter @repo/server build"]
		}
	},
	"caches": {
		"pnpm-store": {
			"directory": "/root/.local/share/pnpm/store",
			"type": "shared"
		}
	},
	"deploy": {
		"startCommand": "NODE_ENV=production bun ./apps/server/dist/index.js",
		"paths": ["node_modules/.bin"]
	}
}
